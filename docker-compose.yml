services:
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "3101:80"
    depends_on:
      - api
    networks:
      - blogkeeper-network
    restart: unless-stopped

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    environment:
      - DOCKER_DEFAULT_PLATFORM=linux/amd64
    ports:
      - "3102:3102"
    networks:
      - blogkeeper-network
    restart: unless-stopped

networks:
  blogkeeper-network:
    driver: bridge